<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: 'navbar-brand' do %>
        <%= fa_icon 'search', text: 'GetItDone' %>
      <% end %>
    </div>

    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= active?(root_path) %>"><%= link_to 'Home', root_path %></li>
        <% if user_signed_in? %>
          <li class="<%= active?(new_task_path) %>"><%= link_to 'Add task', new_task_path %></li>
          <li class="<%= active?(edit_user_registration_path) %>"><%= link_to 'Update Profile', edit_user_registration_path %></li>
          <li><%= link_to "Log out", destroy_user_session_path, method: "delete" %></li>
        <% else %>
          <li>
            <%= link_to new_user_session_path do%>
              <%= fa_icon 'key', text: 'Login' %>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>